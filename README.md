# ColdCraft HQ

High-converting marketing website for ColdCraft HQ — a B2B lead generation agency.

## Stack

- **Next.js 14** (App Router)
- **Tailwind CSS**
- **TypeScript**
- **Framer Motion** (scroll animations)

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Environment Variables

Copy `.env.example` to `.env.local` and fill in your values:

```bash
cp .env.example .env.local
```

## Deployment

Configured for Vercel. Connect your GitHub repo at [vercel.com](https://vercel.com).

## TODO

- Replace Cal.com embed placeholder on `/book` with actual embed code
- Replace placeholder metrics with real data (marked with `TODO` comments)
- Replace placeholder testimonials with real quotes
